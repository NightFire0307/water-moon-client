import CustomModal from '@/components/CustomModal/CustomModal.tsx'

function PreSelectionConfirmModal() {
  return (
    <CustomModal title="预选确认" centered>
    </CustomModal>
  )
}

export default PreSelectionConfirmModal
